<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/main.css">
    <script defer src="js/dashboard.js"></script>
    <title>Dashboard</title>
</head>
    <header>
        <h1 id="role">Role</h1>
        <h1>Hip-Coffee-Co</h1>
        <% if(currentUser) {%>
            <h1><%= currentUser.username %></h1>
        <% } else { %>
            <h1><a href="login" id="employee-name">login</a></h1>
        <% } %>
    </header>
    <main>
        <div class="middle-page">
            <button id="clock-in" action="submit">Clock-In</button>
            <button id="log-out" action="submit">Log-out</button>
        </div>
    </main>
</html>